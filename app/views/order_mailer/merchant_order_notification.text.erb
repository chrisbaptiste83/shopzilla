You Made a Sale! 🎉
Order #<%= @order.id %> · Total: <%= number_to_currency(@order.total) %>
==================================================

Customer: <%= @user.email %>
Date: <%= @order.created_at.strftime("%B %d, %Y at %I:%M %p %Z") %>
Payment / Session ID: <%= @order.stripe_session_id %>

Items:
<% @order.order_items.each do |item| %>
* <%= item.product.title %> (Qty: <%= item.quantity %>) — <%= number_to_currency(item.unit_price) %>
<% end %>

View in Admin: <%= admin_order_url(@order) rescue admin_root_url %>
